server:
  port: 18082

xa:
  mass:
    kernel:
      base-url: http://127.0.0.1:18080
      connect-timeout: 1s
      read-timeout: 5s

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true
      group:
        readiness:
          include: readinessState,kernel
