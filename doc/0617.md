
mass_core工作
* 将mass_core重构成一个独立模块，不是直接启动spring项目，ws server 和client 基础功能是由core提供，需要在其他模块启动，比如mass_mock.比如mass_mock默认启动server 需要启动client需要写在test测试中，
* 解决client、server断线重连，channel 可以找到，做到不丢失、不重复
*  统一client 处理消息也通过队列形式进行处理，仿照server处理消息格式
*  将client和server 处理消息进行抽象，TaskResultHandler 应该是由抽象结仇实现，可以由外部自由实现，core可以提供默认实现，但外部可以自由处理


mass_mock工作
将client TaskResultHandler 可以在 mass_mock中实现，并且注入到client 和server处理消息流程中