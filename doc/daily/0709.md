```java


<T> FilterResult<T> filter(T input, JsonDslDefinition definition, ProcessingContext context);


default <T> FilterResult<T> filter(T data, JsonDslDefinition definition, ProcessingContext context) {
    if (data instanceof List) {
        return filterList((List<T>) data, definition, context);
    }
    //data convertToMap if success
    Map<String, Object> objMap = convertToMap(data);
    if (objMap != null) {
        filterMap(data, definition, context);
    }
    //抛出异常，不支持的类型
}
//返回对象的包装

```

```java
createTask(TaskCreateRequestDto dto);

uploadTaskSeed(String tid); //流式任务可能是多次调用


//等待审核 (如果需要，可能为自动审核)

//审核通过，进入ready 

//device task绑定服务，绑定READY、RUNNING 任务 (如果需要，可能不需要绑定设备)


//判定是否达到发送条件 ，通常是达到最低绑定设备数量 

//生成TaskMsg 
genTaskMsg();

//推送到执行队列
push_geteway_task_queue();

```
