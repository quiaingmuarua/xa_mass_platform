> **⚠️ 已归档 — 请勿参考此文件**
>
> 本文件描述的是 2025 年中期的历史架构。以下类均已不存在：
> - `WebSocketServerStarter` → 已被 `MockApplicationSpringBootApp` 取代
> - `MassServerBuilder` / `MassServerConfig` / `MassServerStater` → 已被 `MassApplicationBuilder` 取代
> - `DispatcherContext(inputQueue, outputQueue, sessionManager, gson)` → 构造函数签名已变更
>
> **当前可用的启动说明见 [`doc/VERIFIED_RUNBOOK.md`](./VERIFIED_RUNBOOK.md)**

---

# [ARCHIVED] XA Mass Platform 启动流程说明（2025 历史版本）

以下内容仅供了解历史演化，**不反映当前代码状态**。

## 1. 启动入口（已废弃）

~~项目采用 Spring Boot，服务端启动入口为~~  
~~`xa-mass-mock/src/main/java/com/xa/mass/mock/runner/WebSocketServerStarter.java`~~

**当前真实入口**：`xa-mass-mock/src/main/java/com/xa/mass/mock/MockApplicationSpringBootApp.java`

---

## 2. 核心启动流程（历史描述）

以下 API 在当前代码中均不存在，仅为历史记录：

- `MassServerBuilder.create().withPort(...).withWebSocketPath(...)...`
- `MassServerStater`
- `WebSocketServerStarter implements CommandLineRunner`

---

> 如需了解当前架构，请参考：
> - [`AGENTS.md`](../AGENTS.md) — agent 快速入口
> - [`doc/VERIFIED_RUNBOOK.md`](./VERIFIED_RUNBOOK.md) — 已实测的启动与验证流程
> - [`doc/AGENT_BASELINE.md`](./AGENT_BASELINE.md) — 模块职责与已知事实
