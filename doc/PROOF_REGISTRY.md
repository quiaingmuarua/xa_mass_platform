# Proof Registry

Last updated: 2026-06-13

Status: current project-level proof ledger.

This file is the single lookup table for critical proof ownership.

Use it to answer:

1. which lane is authoritative for an invariant
2. which integrated scenario is the representative proof
3. which trace analyzer or trace-observed scenario is the canonical
   observational proof
4. which distributed-edge lane also matters
5. where the next proof belongs when current coverage is insufficient

This file does not replace owner matrices. It sits above them and points to the
current authoritative proof surface.

Use with:

- [TESTING_INDEX.md](./TESTING_INDEX.md)
- [TRACE_CONTRACT.md](./TRACE_CONTRACT.md)
- [../xa-mass-engine/doc/baseline/SCHEDULING_CORRECTNESS_MATRIX.md](../xa-mass-engine/doc/baseline/SCHEDULING_CORRECTNESS_MATRIX.md)
- [../xa-mass-engine/doc/baseline/PLATFORM_SCHEDULING_PLANE_POLICY_PROOF_INVENTORY.md](../xa-mass-engine/doc/baseline/PLATFORM_SCHEDULING_PLANE_POLICY_PROOF_INVENTORY.md)
- [../xa-mass-engine/doc/baseline/KERNEL_CONVERGENCE_MATRIX.md](../xa-mass-engine/doc/baseline/KERNEL_CONVERGENCE_MATRIX.md)
- [../xa-mass-trace/README.md](../xa-mass-trace/README.md)

## 0. Proof Rule

Current default is `dual-proof`.

Proof value is not measured by line or branch coverage. A test belongs in this
registry only when it proves at least one of:

- lifecycle or result convergence
- a mainline scheduling, dispatch, retry, release, or recovery mechanism
- a core policy decision that changes eligibility, admission, routing, finality,
  or ownership
- a cross-boundary contract exposed through HTTP, SDK, transport, external
  worker behavior, or trace
- an integrated abnormal path involving concurrency, timing, process restart,
  transport churn, lease expiry, stale result, or distributed runtime state

For critical invariants:

- primary proof is one authoritative deterministic lane
- representative proof is one real-wiring integrated lane
- trace proof is one canonical observational path through `xa-mass-trace`
- policy correctness and lifecycle correctness must start from the primary
  deterministic lane; representative server E2E must not expand into the full
  policy or lifecycle matrix

Do not treat all lanes as equal proof for the same invariant.

Current lane roles:

- `xa-mass-engine` scheduling/kernel suites:
  authoritative deterministic proof
- `xa-mass-server` E2E and parity suites:
  representative integrated proof
- `xa-mass-trace` analyzers and trace-observed scenarios:
  canonical observational proof
- `xa-mass-testing` chaos and soak:
  distributed-edge proof

Project-level proof classes are defined in
[TESTING_INDEX.md](./TESTING_INDEX.md): `Product / API Capability Proof`,
`Policy & Safety Correctness Proof`, and `Scoped Operational Resilience Proof`.
This registry remains the invariant ownership ledger. References below to
specific "classes" or suites mean Java proof classes/suites unless they name
one of those project-level proof classes explicitly. The registry's
highest-priority confidence question is usually "can it be wrong?" because a
usable or fast path is not trustworthy if authorization, scheduling, lifecycle,
or policy bypass can mutate the wrong state.

Use the project proof lines from `TESTING_INDEX.md` when choosing the next test:
`task-producer-api-key`, `worker-api-key`, `operator-admin-session`,
`scheduling-policy-correctness`, `lifecycle-result-correctness`,
`authorization-no-bypass-safety`, `scale-contention-evidence`, and
`fault-recovery-evidence`. Capability proof must name the credential/session
family. Authorization and no-bypass proof must use negative cases; do not treat
a happy product path as evidence that the wrong credential, scope, route
family, or CSRF state fails closed.

Route/auth failures with a correct credential/session, correct route family,
correct scope/project/event, and correct request shape are Product/API
Capability failures. Route/auth success with an incorrect credential, scope,
route family, CSRF state, fixture/dev header, or impersonated worker belongs to
`authorization-no-bypass-safety`.

## 1. How To Use This Ledger

When a new change needs proof:

1. find the nearest invariant id below
2. use the listed primary proof lane first
3. add representative or trace proof only if the listed status says the pair is
   missing or the changed risk really sits on host/runtime or distributed edge
4. do not add server E2E permutations for policy correctness when an engine
   deterministic test can prove the invariant directly
5. do not add a new test in another lane when an authoritative proof class
   already exists for that invariant
6. if the risk is only implementation coverage or a happy-path duplicate, do
   not add a test; strengthen the existing proof or delete the low-value case

Mainline rule:

- if a class is not named here, it is not mainline proof by default
- support or smoke coverage may still exist outside this ledger, but it must not
  be treated as the reason to add another duplicate test in a mainline suite
- tests tagged `secondary-proof` are explicitly downgraded support coverage
  unless they are promoted into this ledger in a later change
- computed-default Scheduling Plane policy facts are classified in
  `xa-mass-engine/doc/baseline/PLATFORM_SCHEDULING_PLANE_POLICY_PROOF_INVENTORY.md`;
  they are not evidence that `SchedulingPolicyCatalog`,
  `ProjectSchedulingBinding`, SDK policy config, policy ids, or multiple policy
  variants exist

When a representative E2E or black-box flow fails:

1. use the trace analyzer or trace-observed scenario listed here
2. then fall back to the primary deterministic proof class
3. do not rediscover the proof graph from scratch

## 2. Critical Invariants

| Invariant id | Invariant statement | Primary proof | Representative integrated proof | Trace proof | Distributed-edge proof | Status | Do not duplicate here |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `sched.worker-eligibility-routing` | eligible worker is selected; wrong worker is excluded under attributes, targets, capability, and lock state | `EngineSchedulingCoreSuite`: `TaskWorkerEligibilityTest`, `TaskSchedulingGateAndTargetingTest` | `ServerSchedulingE2eSuite`: `TaskApiWorkerAttributeRoutingTraceObservedIntegrationTest` | `TaskApiWorkerAttributeRoutingTraceObservedIntegrationTest` + analyzer `worker-attribute-routing-without-context` | add chaos only if disconnect or lease-expiry changes the routing outcome | `covered` | do not add another topic-local engine or server routing test before checking these classes |
| `sched.worker-state-dimensions` | worker-runtime owns one dispatch eligibility decision; reachability observations, readiness/dispatch gate labels, and occupancy/capacity evidence are inputs or diagnostics, not three parallel scheduling truth owners; direct selection-time reachability gates are forbidden; selection-facing `WorkerSchedulingViewRuntime` must not expose reachability; state/command dispatch eligibility policy is worker-runtime owned, and positive state/command recovery must pass through worker-runtime validation instead of directly clearing dispatch gates; production selection acquires bounded eligible worker slots through score-band worker slot state and writes `FUTURE_BAND` claim observations instead of old parallel reservation and claimed/final counters; current score-band proof is one active score-band hold per worker resource and does not prove same-worker concurrent acquisition through old reservation accounting for `declaredCapacity > 1`; claim-close release/final/lock-release evidence may request dispatch wakeup after worker-runtime state changes, but it does not clear unrelated block sources; non-reachable-by-worker-runtime-eligibility, state-report draining/dispatch-disabled, command-drained, locked/exclusive, or future-held worker score-band holds cannot bind work | `EngineSchedulingCoreSuite`: `WorkerStateReportSchedulingIntegrationTest`, `TaskWorkerEligibilityTest`, `TaskSchedulingGateAndTargetingTest`, `TaskSchedulingContentionTest`; support guards and score-band proof `WorkerSelectionContractGuardTest`, `EngineSchedulingCoreArchitectureGuardTest`, `WorkerManagerTest`, `WorkerSelectionAtomicRuntimeTest`, `WorkerSelectionRankingMechanicsTest`, `InMemoryWorkerScoreBandSlotRuntimeTest`, `RedisWorkerScoreBandSlotRuntimeTest`, `SimpleTaskDispatchBinderTest`, `TaskWorkerAssignListenerTest` | representative host proof reuses worker eligibility/routing and retry rows unless server read APIs expose new diagnostic fields | trace proof reuses assignment/retry analyzers unless trace schema gains explicit worker eligibility fields | add chaos only when transport churn or Redis runtime degradation changes the worker-runtime eligibility outcome | `covered-score-band-active-hold-current-slice` | state-report proof must start from `WorkerControlService.applyWorkerStateReport(...)` and observe assignment/lease/dispatch outcome; direct dispatch-gate tests are support only. `DEGRADED`/`OFFLINE`/`READY` reports are diagnostic-only projection in the default policy; target-only readiness values and Redis `worker:meta`/`worker:occupancy` split keys remain residue, and enum/getter/key-name tests are not proof. Score-band runtime proof is the current active-hold state-machine proof through memory/Redis contract tests and focused selection/engine guards: hot-path selection must not return to the retired pre-score-band candidate acquisition path or old parallel reservation/counter truth. This row must not be cited as per-permit capacity proof or as proof that future non-exclusive early claim-close policy is implemented; that future belongs to task policy / engine claim-close semantics, not old worker reservation counters. Redis deadline-aware registry lifecycle remains migration/support proof, not the production acquire owner after the score-band pivot. Positive recovery proof must include guards/tests that fail if `DefaultWorkerDispatchAvailabilityPolicy` directly calls `clearWorkerDispatchDisable(...)` or if state/command/release evidence clears unrelated block sources. `doc/archive/core/2026-06-11_WORKER_RUNTIME_ADMISSION_AND_REDIS_SHAPE_CONVERGENCE_ROADMAP.md` preserves the previous platform-side admission proof while it converged SPI and Redis physical shape. Replacing platform-side admission proof with worker-offer outcome proof is deferred to a future Worker Offer Admission roadmap through a real external worker path. |
| `sched.min-worker-gate` | `minRequiredWorkerCount` blocks half-dispatch and keeps the task out of partial start | `EngineSchedulingCoreSuite`: `TaskSchedulingGateAndTargetingTest` | `ServerSchedulingE2eSuite`: `TaskApiMinimumWorkerGateTraceObservedIntegrationTest` | `TaskApiMinimumWorkerGateTraceObservedIntegrationTest` + analyzer `assignment-min-worker-gate` | only add chaos if the risk is degraded presence or lease churn around the gate | `covered` | do not add another plain gate test before checking this engine/E2E/trace chain |
| `sched.retry-redispatch` | retry, lease-expiry, or Redis runtime owner restart/reconnect recovery returns work to competition without double assignment and without stale takeover | `EngineSchedulingCoreSuite`: `TaskRedispatchCompetitionTest`, `TaskSchedulingContentionTest` | `ServerSchedulingE2eSuite`: `TaskApiRetryRedispatchTraceObservedIntegrationTest` as the representative host/runtime retry-scheduler proof on the sealed `SESSION` keep-open path; Redis backend profile coverage must reuse this invariant rather than opening a new proof id | `TaskApiRetryRedispatchTraceObservedIntegrationTest` + analyzer `assignment-retry-redispatch` for assignment retry; lease-expiry takeover and Redis-backed runtime owner restart/reconnect use analyzer `lease-expiry-redispatch` | `xa-mass-testing`: `SdkPollingLeaseExpiryRedispatchChaosRunner`, `SdkWebSocketLeaseExpiryRedispatchChaosRunner`, and `SdkPollingRedisRestartRecoveryChaosRunner`, all bound to analyzer `lease-expiry-redispatch` through the chaos trace planner where the runner has a stable trace sequence contract; Redis process kill, partition, and failover remain scheduled infra-fault work, not current proof | `covered` | do not add another local contention, happy-path redispatch, or Redis-restart-specific proof line before checking this engine/E2E/trace chain |
| `sched.policy-binding-entry-bypass` | retry/wakeup, runtime-ready pump, and lease-expiry redispatch entries must re-enter policy-sensitive worker selection before binding and must not side-door bind a disallowed worker | `EngineSchedulingCoreSuite`: `TaskSchedulingBindingEntryBypassTest` | representative host proof reuses scheduling E2E rows above unless a production entry is rewired outside the engine owner | trace proof reuses assignment/dispatch/lease analyzers from routing and retry rows; PP-4 closure records that no dedicated policy-entry trace bundle is currently required | add chaos only if a distributed trigger can bind work without the engine assignment owner | `covered-engine-pp3` | do not add direct-assignment-only, selector getter, or grep/path tests as binding-entry proof |
| `sched.background-sharing` | released worker resources can be reused by later ready work after claim close/resource release; future non-exclusive task policy may choose an earlier claim-close point after dispatch acceptance, but simultaneous remaining-permit backfill for an occupied worker is not current score-band proof and is not the current mainline target | `EngineSchedulingCoreSuite`: `TaskResourceReleaseListenerTest`, `TaskSchedulingContentionTest` | `ServerSchedulingE2eSuite`: representative sharing scenario carried by real host/runtime wiring where it still matches release/reuse semantics | `TaskApiBackgroundWorkerSharingTraceObservedIntegrationTest` + analyzer `background-worker-sharing` should be rechecked before citing it as simultaneous capacity proof | add chaos only if disconnect or delayed result changes release/reuse behavior | `covered-release-reuse-current-slice` | do not add projection-first sharing checks; do not cite this row as proof that an occupied worker with remaining declared capacity can receive another assignment under score-band active-hold v1 |
| `sched.cross-task-fairness` | one task must not monopolize a reusable worker when another ready task is eligible to compete | `EngineSchedulingCoreSuite`: `TaskSchedulingContentionTest`, `TaskDelayedAvailabilitySchedulingTest` | `ServerSchedulingE2eSuite`: `TaskApiMultiTaskAssignmentIntegrationTest` | `TaskApiCrossTaskWorkerFairnessTraceObservedIntegrationTest` + analyzer `cross-task-worker-fairness` | add soak/chaos only when pressure or fleet timing is the actual risk | `covered` | do not add another host-level fairness test without checking this pair first |
| `sched.late-worker-backfill` | queued ready work dispatches once a worker later becomes eligible or joins the fleet | `EngineSchedulingCoreSuite`: `TaskDelayedAvailabilitySchedulingTest` | `ServerSchedulingE2eSuite`: `TaskApiDelayedWorkerAvailabilityTraceObservedIntegrationTest` | `TaskApiDelayedWorkerAvailabilityTraceObservedIntegrationTest` + analyzer `late-worker-backfill` | `xa-mass-testing` polling soak late-worker profile | `covered` | do not add another delayed-availability unit test before checking this chain |
| `transport.endpoint-lease` | endpoint lease truth is keyed by opaque `deliveryBucketId + workerId` endpoint evidence, not by route-owner scans or worker lifecycle state; current SDK/starter route minting is worker-group-level and outside transport runtime; WebSocket handshake identity is normalized as `AdapterSessionIdentity(deliveryBucketId, workerId)` at the protocol/evidence edge and then unwrapped into the current server handle/registry boundary; stale session tokens cannot revoke newer endpoint leases; adapter-mailbox delivery target evidence is post-selection assignment infrastructure and does not carry reachability state: embedded starter may derive it from worker registration plus local transport binding, split engine-producer runtime must inject it explicitly, and neither path reads endpoint leases or session presence; dispatch queue availability is finite mailbox consumer availability evidence, not endpoint lease projection; assigned-delivery producer queue selection and adapter-owned final-hop dispatch do not read endpoint lease state; transport delivery does not decide worker capacity, worker lifecycle, worker-runtime reachability, registry slot heartbeat, retry, reassign, compensation, attempt timeout, or final recovery; there is no transport-to-worker-runtime session-presence event bridge; connected/heartbeat observations stay transport-local and must not refresh registry-owned slot heartbeat, request dispatch wakeup/recheck, or open worker eligibility; confirmed current-session disconnect may best-effort block worker dispatch through the worker-runtime negative-only block port, while pre-transport missing target, final-hop NO_ENDPOINT, backpressure, invalid, shutdown, mailbox/system unavailable, and generic failed outcomes remain delivery outcomes and do not block workers in the current roadmap | transport runtime/API proof: `CanonicalWorkerGroupRouteKeyCodecTest` for the SDK/starter default worker-group route-key codec, `RedisTransportEndpointLeaseStoreTest`, `InMemoryTransportEndpointLeaseStoreTest`, `TransportAssignedDeliverySubmitterTest`, `AdapterMailboxConsumerLoopTest`, `InMemoryTransportDispatchHandoffTest`, `TransportDispatchQueueContractTest`, `TransportDispatchBatchCodecTest`, `PollingDeliveryExecutorTest`, `InMemoryPollingPendingDeliveryBufferTest`, `RedisTransportDispatchHandoffTest`, `TransportConvergenceArchitectureGuardTest`, `TransportRedisKeyspaceGuardTest`, `DispatcherInboundHandlerTest`, plus worker-runtime/starter registry, delivery-target, and negative-block proof `TaskDispatchRoutingSubmitterTest`, `MassApplicationDistributedTransportTest`, `WorkerManagerTest`, `InMemoryWorkerRegistryTest`, and `RedisWorkerRegistryTest` | SDK/server representative proof: `MassApplicationDistributedTransportTest`, `ExternalWorkerApiControllerTest`, `ExternalWorkerRealtimeRegistrationIntegrationTest`, plus public Java SDK `WorkerClientTest`, `PollingWorkerRuntimeTest`, `WebSocketWorkerRuntimeTest`; `ExternalWorkerPollingApiIntegrationTest#externalWorkerPollingApiCompletesTaskEndToEnd` is representative worker data-plane proof, while endpoint diagnostics, transport freshness, mailbox consumer evidence, and registry slot heartbeat remain separate owner proofs | no dedicated trace analyzer currently; endpoint lease proof is runtime/transport/API behavior until trace schema exposes endpoint lease events | add chaos only for real distributed endpoint churn, Redis endpoint lease failure, or adapter process takeover beyond current deterministic proof | `covered-dispatch-routing-handoff` | do not use raw source scans, field-copy tests, workerId route resolver helpers, worker-id inspection views, or route-owner stores as endpoint lease proof; transport Redis must keep old route-owner `route:<encodedRouteKey>:consumers`, `workers`, `owner-shards`, `worker-routes:*`, `worker-route:*`, `routes`, `route-presence:*`, old `adapter:<adapterId>:worker:<workerId>:owner` pointers, `bucket:<deliveryBucketId>:worker:<workerId>:owner` pointers, and worker runtime/admission keys out of transport truth |
| `transport.dispatch-message-ownership` | assigned dispatch messages are minimal delivery intents: the item-level correctness target is selected worker plus opaque worker payload, opaque delivery correlation, and item timing/id only; the physical handoff target is carried once as `adapterMailboxKey` on `AdapterMailboxDispatchBatch`, not inside each item. Known offer rejection, unavailable mailbox, invalid/corrupt dispatch message, missing selected-worker endpoint, and adapter final-hop failure must return `DispatchOutcome` rather than disappearing into logs or diagnostics. Accepted items with no later worker consumption, process completion, or result are engine-owned task attempt timeout/retry concerns, not transport retry loops. Task shell metadata such as task name, project, and user id, plus `deliveryBucketId`, `adapterId`, `deliveryQueueKey`, `targetTransportNodeId`, endpoint lease evidence, connection/session facts, worker pull DTOs, task-shaped message/attempt fields, and generic task-dispatch packet carriers are owned outside dispatch message payloads by worker-runtime delivery target evidence, adapter-mailbox batch key, dispatch queue references, mailbox consumer context, adapter-local final-hop send, or SDK/server public projection | transport runtime/API proof: `PulledDeliveryMessageTest`, `DeliveryPullResultTest`, `DispatchOutcomeTest`, `DispatchMessageTest`, `AdapterMailboxDispatchBatchTest`, `TransportDispatchBatchCodecTest`, `TransportDispatchQueueContractTest`, `InMemoryTransportDispatchHandoffTest`, `RedisTransportDispatchHandoffTest`, `PollingDispatchMessageCodecTest`, `InMemoryPollingPendingDeliveryBufferTest`, `TransportConvergenceArchitectureGuardTest` | SDK/server representative proof: `MassApplicationDistributedTransportTest`, `ExternalWorkerApiControllerTest`, `ExternalWorkerPollingApiIntegrationTest#pollingWorkersSharingRouteAndQueueCannotCrossConsumeSelectedWorkerItems`, plus public Java SDK `WorkerClientTest`, `EmbeddedPullWorkerSessionTest`, starter-owned `TaskDispatchPayloadEncoderTest` / `TaskDispatchDeliveryCorrelationCodecTest`, and worker-pull `WorkerActionPayloadDecoderTest` | no dedicated trace analyzer currently; this is a transport process-boundary shape invariant until trace schema exposes dispatch message records | add chaos only if distributed queue serialization, mailbox consumer evidence churn, or adapter-node takeover can corrupt dispatch message fact ownership | `covered-explicit-channel-carrier-dispatch` | do not prove this with old field-copy tests, broad source scans alone, nested worker pull DTOs, packet-backed dispatch envelopes, Redis value queue-key copies, fabricated unknown route/node fields, compatibility delivery-command wrappers, generic routing-target carriers, or a generic structured `correlation` bag |
| `transport.embedded-adapter-independence` | transport core is queue/evidence/outcome/result-ingress runtime, while embedded Java adapter startup is owned by `transport/adapter-starter`. Embedded SDK passes `EmbeddedAdapterRuntimeSpec` values; the starter owns factory-by-type, runtime-by-adapter-id, adapter-id lifecycle, and binding registry creation without exposing runtime sets, contribution baskets, queue objects, or concrete adapter internals. Concrete adapter runtime factories consume spec plus shared runtime environment ports and must not receive raw endpoint lease stores, worker-runtime scheduling mutation surfaces, generic delivery services, mailbox registries, or handoff internals. | transport runtime proof: `TransportConvergenceArchitectureGuardTest`, `TransportRuntimeRegistryTest`, `TransportRegistrationResolverTest`, `AdapterDispatchQueueConsumerLoopTest`, `CompositeEmbeddedTransportAdapterRuntimeTest`, `EmbeddedAdapterStarterTest`, `WebSocketAdapterRuntimeFactoryTest`, `SocketAdapterRuntimeFactoryTest`, `PollingDeliveryExecutorTest`, `PollingDeliveryPullChannelTest`, and `PollingSessionEvidenceDriverTest`; guards ensure legacy embedded bootstrap/contribution/availability classes do not reappear and core delivery/lease packages do not import embedded assembly | SDK/server representative proof: `MassSdkTest`, `TransportConfigTest`, `MassApplicationDistributedTransportTest`, plus source guards that keep legacy embedded bootstrap/factory surfaces out of external SDK, public-contract, server worker API, and integration worker surfaces | no dedicated trace analyzer currently; this is an architecture ownership invariant, not a runtime trace sequence | add distributed-edge proof only when a real external or cross-language adapter protocol becomes product work | `covered-embedded-adapter-starter` | do not treat embedded Java starter APIs as remote adapter APIs, move Java callback contracts into `transport_api`, let concrete adapters mint mailbox keys or own worker-to-mailbox placement, add adapter health monitor loops to command drain/final-hop paths, or add wrapper/facade seams that only hide the same Java object ownership |
| `transport.adapter-command-executor` | assigned-delivery adapter execution is a local embedded Java `AdapterCommandExecutor.dispatch(List<DispatchMessage>)` seam owned by runtime embedded adapter support, not by `transport_api`; adapter runtime queue consumers drain one explicit dispatch queue key and call the concrete adapter executor. Adapter id, adapter mailbox key, transport hint, protocol label, pull channel, pull-session evidence driver, server lifecycle, and diagnostics are explicit `TransportBinding` or runtime factory facts, not command executor facts. Polling uses separate `PollingDeliveryExecutor`, `PollingDeliveryPullChannel`, and `PollingSessionEvidenceDriver` objects; WebSocket and Socket create runtime executors from final-hop `send(DispatchMessage)` functions that frame public worker-channel ACTION payloads and call adapter-local selected-worker session lookup. | transport runtime proof: `TransportConvergenceArchitectureGuardTest`, `AdapterCommandExecutorsTest`, `AdapterDispatchQueueConsumerLoopTest`, `CompositeEmbeddedTransportAdapterRuntimeTest`, `TransportRuntimeRegistryTest`, `TransportRegistrationResolverTest`, plus adapter command-path proof `WebSocketAdapterRuntimeFactoryTest`, `SocketAdapterRuntimeFactoryTest`, `PollingDeliveryExecutorTest`, `PollingDeliveryPullChannelTest`, `PollingSessionEvidenceDriverTest`, `WebSocketSessionRegistryTest`, `WebSocketSessionEvidenceRefresherTest`, and `SocketSessionManagerTest` | SDK/server representative proof: `MassApplicationDistributedTransportTest` and embedded SDK starter tests that assemble specs and runtime factories for WebSocket, Socket, and Polling adapters | no dedicated trace analyzer currently; this is a local transport assembly and adapter command boundary invariant | add distributed-edge proof only when adapter process takeover or remote adapter registration becomes a product feature | `covered-adapter-command-executor-current-slice` | do not resurrect `WorkerAdapter` or `PollingWorkerAdapter` as compatibility facades, put `AdapterCommandExecutor` back in `transport_api`, derive binding metadata from executors, pass endpoint lease/session/route/raw facts into command executor inputs, duplicate endpoint lease/presence projection inside command executors or pull channels, duplicate push adapter outcome loops after the shared template exists, reintroduce protocol-specific executor wrapper classes that only delegate to `AdapterCommandExecutors`, or hide runtime outputs in mutable context setters |
| `transport.push-selected-worker-delivery` | WebSocket/socket task dispatch preserves the engine-selected worker: push final-hop send is owned by the concrete adapter command executor and must use the command `selectedWorkerId` without adapter/route/connection ids or `deliveryBucketId` as local delivery correctness; WebSocket creates that executor from a final-hop send function which writes frames directly after `WebSocketSessionRegistry` worker-id-only lookup, Socket creates that executor from a final-hop send function which writes frames directly after adapter-local `SocketSessionManager` worker-id lookup, WebSocket and Socket session state are indexed by worker id plus protocol-local connection identity, route-only raw helpers are not assigned-delivery fallbacks, and adapters must not scrape packet payload worker id fields for endpoint addressing; server/session wiring receives protocol-specific session handles instead of a selected-worker endpoint registry | transport runtime/API proof: `WebSocketAdapterRuntimeFactoryTest`, `SocketAdapterRuntimeFactoryTest`, `WebSocketSessionRegistryTest`, `SocketSessionManagerTest`, `TransportConvergenceArchitectureGuardTest` | SDK/server representative proof: `MassApplicationDistributedTransportTest`, `ExternalWorkerRealtimeRegistrationIntegrationTest`, public Java SDK `WebSocketWorkerRuntimeTest` | no dedicated trace analyzer currently; push selected-worker proof is transport/API behavior until trace schema exposes selected-worker endpoint delivery facts | add chaos only if realtime adapter reconnect, route takeover, or split transport-node failure can change selected-worker preservation | `covered-selected-worker-push-current-slice` | do not prove this invariant with route-only direct-send tests, bucket-worker local lookup, packet payload workerId extraction, adapter-id endpoint send arguments, same-shape `WorkerEndpointRegistry` wrappers, protocol-specific executor wrappers that only delegate to the runtime helper, or broad endpoint scans under shared routeKey |
| `transport.polling-selected-worker-delivery` | polling task delivery preserves the engine-selected worker: adapter mailbox is the physical queue address, `selectedWorkerId` is a queued value demux constraint and pull selector, and two polling workers sharing one routeKey and one adapter mailbox cannot consume each other's assigned task items; polling pending pull-buffer enqueue, pull demux, and session evidence are separate adapter capabilities | transport runtime/API proof: `InMemoryPollingPendingDeliveryBufferTest`, `PollingDispatchMessageCodecTest`, `PollingDeliveryExecutorTest`, `PollingDeliveryPullChannelTest`, `PollingSessionEvidenceDriverTest`, `EmbeddedPullWorkerSessionTest`, `TransportConvergenceArchitectureGuardTest` | server/SDK representative proof: `ExternalWorkerApiControllerTest`, `ExternalWorkerPollingApiIntegrationTest#pollingWorkersSharingRouteAndQueueCannotCrossConsumeSelectedWorkerItems`, public Java SDK `WorkerClientTest`, `PollingWorkerRuntimeTest`, `JavaExternalSdkPollingSessionIntegrationTest` | no dedicated trace analyzer currently; polling selected-worker proof is polling-adapter/transport API behavior until trace schema exposes selected-worker delivery events | add chaos only if adapter-node locality or Redis queue failure can change selected-worker preservation | `covered-selected-worker-polling` | do not implement proof by routeKey-only polling, worker-specific routeKey minting, selected-worker transport handoff keys, poll-and-discard filtering, packet payload workerId extraction, Redis value queue-key copies, mixed polling adapter objects, or endpoint lease presence as a post-assignment routing engine |
| `transport.result-ingress-entry` | worker result ingress uses `ResultIngressEntry(partitionKey=<resultCorrelationRef>, message)` as the transport queue/process-boundary carrier; transport and concrete adapters route, enqueue, and diagnose the entry without parsing task-result success, result code, task id, message id, attempt id, lease token, retry policy, or finality; after protocol-specific extraction, `AdapterResultIngressEntries` owns generated message id, default deadline/timestamp, partition key, message construction, diagnostics copying, and required correlation/payload validation; starter-owned `TaskResultCallbackCodec` decodes the opaque payload and validates `message.resultCorrelationRef` against payload `resultCorrelationRef` before engine result apply | transport runtime/API proof: `ResultIngressEntryTest`, `ResultIngressMessageTest`, `AdapterResultIngressEntriesTest`, `AdapterInboundResultProcessorTest`, `WorkerChannelActionReplyResultFrameReaderTest`, `InMemoryTransportResultIngressQueueTest`, `RedisTransportResultIngressChannelTest`, `TransportConvergenceArchitectureGuardTest`, plus adapter diagnostics/frame proof `JsonAdapterResultDiagnosticsProviderTest`, `WebSocketAdapterRuntimeFactoryTest`, `DispatcherInboundHandlerTest`, `SocketTransportServerTest`, and `SocketTransportFrameCodecTest` | SDK/server representative proof: `TaskResultCallbackCodecTest`, `TaskResultIngressQueueDrainTest`, `RuntimeTaskResultIngestChannelTest`, `EmbeddedPullWorkerSessionTest`, `ExternalWorkerApiControllerTest`, public Java SDK `WorkerClientTest`, `PollingWorkerRuntimeTest`, `WebSocketWorkerRuntimeTest`, and `JavaExternalSdkArchitectureGuardTest` | no dedicated trace analyzer currently; result carrier proof is transport/API behavior until trace schema exposes result ingress events | add distributed-edge proof only when Redis result ingress queue failover or external adapter result relay becomes a distinct risk | `covered-explicit-channel-carrier-result` | do not prove this with `TransportResultIngressEnvelope`, generic routing envelopes, adapter parsing of `success`, result-code routing, engine-targeted result payload labels, stats/list/count surfaces, duplicated adapter-side `ResultIngressEntry` construction, or broad source scans without carrier/codec/bridge tests |
| `kernel.duplicate-callback-idempotence` | duplicate callback replay is accepted idempotently and does not reopen or corrupt terminal work | `EngineKernelConvergenceSuite`: `TaskResultRuntimeConvergenceTest`, `TaskResultConcurrencyConvergenceTest` | `ServerLifecycleResultConvergenceSuite`: `TaskApiCallbackReplayTraceObservedIntegrationTest` | `TaskApiCallbackReplayTraceObservedIntegrationTest` + analyzer `duplicate-callback-replay` | `xa-mass-testing`: `SdkWebSocketLateResultAfterLeaseExpiryChaosRunner` for reconnect + stale late replay, bound to analyzer `late-stale-result-replay` | `covered` | do not add another projection replay test before checking the runtime-first lifecycle/trace pair |
| `kernel.result-terminal-convergence` | final result closes aggregate and runtime truth consistently for success, all-failed, and mixed-result paths | `EngineKernelConvergenceSuite`: `TaskResultRuntimeConvergenceTest`, `TaskResultConcurrencyConvergenceTest`, and `TaskIdleClosePolicyBehaviorTest` | `ServerLifecycleResultConvergenceSuite`: `TaskApiFailureResultIntegrationTest`, `TaskApiMixedResultsTraceObservedIntegrationTest`, `TaskApiAllMessagesFailedTraceObservedIntegrationTest` | `TaskApiAllMessagesFailedTraceObservedIntegrationTest` + analyzer `all-failed-terminal-convergence`; `TaskApiMixedResultsTraceObservedIntegrationTest` + analyzer `mixed-result-terminal-convergence`; single-success remains covered by analyzer `single-message-success` | `xa-mass-testing`: result-shape chaos runners are scheduled/manual support unless a distinct distributed-edge invariant is justified | `covered` | do not add another result-topic variant before checking this engine/lifecycle/trace chain |
| `kernel.resource-release-reuse` | normal terminal closure or retry-exhausted closure releases worker/resource ownership so later ready work can acquire it | `EngineKernelConvergenceSuite`: release and cleanup deterministic coverage | `ServerSchedulingE2eSuite`: `TaskApiSingleWorkerReuseTraceObservedIntegrationTest` | `TaskApiSingleWorkerReuseTraceObservedIntegrationTest` + analyzer `worker-resource-cleanup-without-context` | chaos only when disconnect or expiry interferes with cleanup | `covered` | do not add another reuse assertion that only reads projection or piggybacks on unrelated routing proof |
| `ext.worker-parity-public-contract` | external workers preserve the public scheduling/result contract; Java proof is SDK-first through scenario-launcher, while Node samples remain adapter fixtures | `ExternalWorkerParitySuite` with dedicated external-worker parity classes as the primary integrated proof surface | `ExternalWorkerPublicContractTraceObservedIntegrationTest`, `ExternalWorkerPollingApiIntegrationTest`, `NodePollingWorkerBlackBoxIntegrationTest`, `NodeWebSocketWorkerBlackBoxIntegrationTest`, `NodeSocketWorkerBlackBoxIntegrationTest`, and `JavaScenarioLauncherBlackBoxIntegrationTest` | `ExternalWorkerPublicContractTraceObservedIntegrationTest` + analyzer `external-worker-public-contract-success` | transport/disconnect edges live in websocket/socket chaos and black-box runners; Java socket is not a public SDK session | `covered` | do not add per-adapter duplicate parity happy-path tests before checking this unified parity suite and trace proof |
| `ext.java-sdk-task-scoped-invocation` | external Java task producers can create a WorkerGroup-scoped `SESSION` task, approve it while intake stays open, synchronously append one item through a task-scoped SDK handle, and read results without a hidden invoke route or server-side task auto-creation | `TaskClientTest` covers typed routing helper JSON/merge semantics and `TaskHandle` route delegation | `JavaExternalSdkTaskScopedInvocationIntegrationTest` | trace-observed coverage belongs to `ext.worker-parity-public-contract` unless task-scoped invocation adds a distinct trace invariant | add distributed proof only when SDK task-scoped invocation depends on reconnect, timeout, or multi-worker contention behavior | `covered` | do not add `mass.invoke(...)` or one-task-per-item happy-path tests when this task-scoped proof covers the public SDK route |
| `ext.worker-pack-sdk-capability` | worker-pack capability groups can run as external Java SDK workers without privileged server startup or raw Java transport demos | `GeoLookupToolTest`, `ProbeWorkerPackTest`, and `WorkerPackArchitectureGuardTest` for provider, handler, group, and package-boundary contracts | `WorkerPackGeoLookupExternalSdkIntegrationTest` and `PhoneDeviceWorkerPackExternalSdkIntegrationTest` | trace-observed coverage belongs to `ext.worker-parity-public-contract` unless worker-pack adds a distinct fault/capability trace invariant | add distributed proof only when a real worker-pack capability depends on external provider timing, network churn, or Stage-2 worker/device selection | `covered` | do not add worker-pack raw transport demos as proof when an SDK-backed capability test covers the public worker path |

## 2.1 Retained Mainline Support Cases

These classes intentionally stay in mainline server suites, but they are not
independent invariant rows. They are retained boundary/support proof under the
nearest mainline owner above and should not be cloned into new topic-local
tests.

| Class | Current reason to retain in mainline suites | Do not duplicate here |
| --- | --- | --- |
| `TaskApiLifecycleGuardsIntegrationTest` | lifecycle shell transition guards on the real host path | do not add another broad shell guard test when this class already covers the remaining host-side transition seams |
| `TaskApiBlockedRunningIntegrationTest` | blocked-running host/runtime lifecycle interaction on real dispatch callbacks | do not add another pause/block shell test unless it introduces a new kernel invariant |
| `TaskApiPauseCompletionIntegrationTest` | paused-running closure path on the real host/runtime wiring | do not add another callback-after-pause shell test without a new invariant |
| `TaskApiResumeAndCompleteIntegrationTest` | paused task resumes after late worker availability on the real host shell | do not add another resume-after-worker-connect shell test before checking this class |
| `TaskApiTerminateRunningIntegrationTest` | terminate-after-assignment shell boundary on the real callback path | do not add another generic terminate-running shell test before checking this class |
| `TaskApiWorkerWithoutContextIntegrationTest` | worker-without-context host boundary coverage remains part of routing/mainline viability | do not add another compatibility-free worker smoke unless registry proof ownership changes |

## 3. Coverage Notes

This registry does not maintain a central gap index. When proof is insufficient,
record the current evidence boundary in the owning baseline, suite map, or
owner README instead of accumulating closed-gap or target-state notes here.

## 4. Naming Rule

When adding a new high-value scenario:

- name the invariant or scenario explicitly
- link the class or suite from this file in the same change
- if the scenario claims trace coverage, name the analyzer id here as well
- if the scenario only supports another proof lane, say so here instead of
  presenting it as a new primary proof

This file is the only default ledger for current proof ownership.
