# Manual Debug Chat Protocol

This document defines the lightweight worker debug-chat protocol used by the status page.

## Purpose

- Provide a human-friendly debug conversation UI on the worker page
- Keep the payload JSON directly visible for internal troubleshooting
- Make delivery state observable without coupling the flow to task lifecycle logic

## Outbound message

- `msgType`: `CONTROL`
- `subMsgType`: `manual-chat`
- `from`: `SERVER`

Payload fields:

- `messageKind`: `debug_chat`
- `text`: debug text content
- `workerId`: target worker id
- `sentAt`: epoch milliseconds
- `expectReply`: `true`
- `clientMessageId`: outbound message id generated by server
- Additional fields may be included for ad-hoc debugging

## Inbound acknowledgement

- `msgType`: `EVENT`
- `subMsgType`: `manual-chat`
- `from`: `CLIENT`

Payload fields:

- `messageKind`: `debug_chat_ack`
- `replyToMessageId`: outbound message id being acknowledged
- `ackStatus`: `RECEIVED`
- `message`: free-form acknowledgement text
- `workerId`: worker id producing the reply
- `receivedAt`: epoch milliseconds
- `echoPayload`: optional echoed request payload
- `echoSubMsgType`: original outbound `subMsgType`

## Page state mapping

- `QUEUED`: server accepted and enqueued outbound message
- `DELIVERED`: acknowledgement matched outbound message id
- `RECEIVED`: inbound acknowledgement event stored in history

## Notes

- This protocol is intentionally separate from `TaskMsg` lifecycle and must not mutate task state.
- A real worker implementation should treat this as a debug/control side-channel, not as business task execution.
