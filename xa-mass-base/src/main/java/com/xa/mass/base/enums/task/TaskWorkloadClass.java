package com.xa.mass.base.enums.task;

/**
 * Declares the engine workload class for a task.
 *
 * <p>This models runtime optimization intent rather than the task's business
 * domain or source shape.
 */
public enum TaskWorkloadClass {
    INTERACTIVE,
    BULK
}
