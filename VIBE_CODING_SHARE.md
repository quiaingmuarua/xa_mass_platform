
---

# 复杂项目里的 Vibe Coding

这份内容不是讲 AI 写了多少代码，而是讲一个更实际的问题：

在一个已经有明显复杂度的项目里，怎么把 AI 协作组织成一个可以持续推进、同时还能不断收敛的工程系统。

---

## 项目现在大概做到什么程度

这个项目是一个通用的分布式任务调度平台。它不是简单 CRUD 后台，也不是单一 worker demo。现在已经有几条比较完整的能力线：

- **任务主线**：`Task shell -> item append -> runtime enqueue -> dispatch -> result convergence -> task state`
- **生命周期和状态机**：不只是成功/失败，还有 terminal reason、final reason、retry、expiry 这些语义
- **runtime 能力**：ready queue、active lease、retry scheduling、watchdog、过期补偿
- **transport 能力**：polling、websocket、socket，三个适配器并行
- **接入方式**：SDK-first，HTTP / server shell 做验证
- **验证体系**：E2E、integration、concurrency、perf、chaos、CI

规模上，最近一个月的工作日，每天 30+ commits，累计 150+ commits，涵盖 15 个 Maven 子模块。这个密度是真实的，不是堆数量，每条 commit message 都是百字以上的完整描述。

所以这个项目消耗 token 的原因，不是单纯代码多，而是**语义密度高**。很多讨论不是"接口怎么写"，而是：

- 真相到底放 runtime、storage 还是 trace
- 生命周期语义有没有漂
- transport shape 会不会反向污染 kernel
- 旧路径是不是还在持续影响新改动

---

## 为什么这个阶段特别适合讲 AI 协作

这个项目现在更像是：

- 主线已经比较清楚
- 能力已经不弱
- 测试和文档基线已经开始成形
- 但结构收敛和边界治理仍然是日常工作的一部分

项目太早，很多经验只是探索；项目太后，很多问题已经被流程盖住了。**这个中间阶段，AI 的收益和问题都比较真实**，踩过的坑和总结出的方法，是可以用的。

---

## 我有几个比较明确的前提

### 1. agent 是 owner，不是被动执行者

我不希望 agent 只是接命令然后写代码。复杂项目里，agent 必须像 owner 一样去理解：当前主线、当前边界、当前风险、当前验证方式。

这个要求比听起来更难落地。agent 默认倾向于"完成任务"，而不是"判断这件事该不该做"。要让它真的扮演 owner，需要持续在约束文档里明确写出来。

### 2. 先形成判断，再进入实现

我不希望 agent 先长时间争论，再迟迟不进入实现。更准确的做法是：先形成判断，复杂问题先暴露理解模型，必要时指出冲突，主线明确后尽快实现和验证。

"argue before coding" 是个伪命题。真正的问题不是争不争，而是：**它有没有理解清楚就开始动手**。

### 3. engine 不是 CRUD backend

只要把 engine 误看成普通 backend，就很容易机械套 `controller/service/repository` 思路，把 runtime kernel 的主线冲淡。这个判断我写进了 AGENTS.md，因为它太容易被忽视了。

### 4. 不要机械按 class 大小驱动重构

内部 orchestrator 可以大。大不自动等于 God class。如果 owner boundary 清楚、主线清楚、调度入口清楚，那就可能是合理的大类。`TaskManager` 实现多个引擎 seam 是当前 owner 设计，不是证明需要再加一个内部 bridge 层的理由。

### 5. 没有真实边界，就不要拆 bridge / facade / wrapper

如果新层没有带来新的 owner boundary、协议边界、生命周期边界或稳定调用面，那大概率只是把调用关系打散。这一条我在 AGENTS.md 里专门加了一个"Abstraction Test"：**如果一个新层不改变谁做决策、谁可以调用、它保护什么生命周期边界，那它大概不该存在。**

---

## 我是怎么推进得比较快的

我这里说的"放开让 agent 写"，不是让它乱写，而是在有约束的前提下放大推进速度。

### 1. 稍复杂的修改尽量先走 plan mode

不是所有改动都开 plan，但只要改动稍微复杂、影响面开始扩大，我会更倾向于先让 agent 写计划。重点不是形式，而是看它是否真的理解：scope、影响面、风险点、验证方式。如果它的 plan 里这四项都模糊，那往往是理解没到位，不宜直接进实现。

### 2. 一个 session 尽量只专注一个模块

我尽量不让一个 session 同时在 engine、transport、storage、server 之间频繁切换。好处很直接：上下文更稳、owner boundary 不容易混、token 更省、agent 记忆更不容易乱。如果确实要跨模块，我会单开一个 session。

### 3. 保留一个全局 session

虽然我强调模块 session，但同时一定保留一个全局 session。局部 session 容易优化局部，而复杂项目更难的是全局判断——比如"这个改动会不会让 transport 的概念渗入 engine kernel"，这种问题需要全局视角。

### 4. 单独开 session 做文档 / 架构 review

这个 session 不负责具体实现，只负责看：文档是不是还代表当前 reality，baseline 是否和代码一致，某次重构之后错误概念有没有继续固化。

实际上这一个月，我在每次大的重构之后都会跑一遍文档 review，发现有不少地方文档比代码落后了两三个版本。**文档和代码之间的 drift，是 AI 协作里最隐蔽的问题之一。**

### 5. 定期用新 agent / 新 session 做第三方 review

长 session 很容易形成路径依赖。新开一个 agent 的价值，不只是 code review，而是重新判断：这个项目现在是否还 agent-friendly、一个新上下文能不能快速抓住主线、某些约束是不是只存在于旧 session 的记忆里。

**这也是我写 AGENTS.md 最直接的动机之一**：它本质上是给每一个新 session 的快速启动文档，让它不需要从头理解三个月的决策历史。

### 6. 一定让 agent 结合动态验证

不能让 agent 只是看代码、改代码、靠静态阅读判断对错。复杂项目里必须尽量让它结合：测试执行、集成验证、日志、trace、状态审计。没有运行时反馈，agent 很容易在静态层面自洽，但实际行为已经漂了。

---

## 我一直比较坚持的一件事：单一真相

复杂系统里，如果同一类事实长期有两三个真相来源，AI 协作几乎一定会越改越乱。

这个项目的架构里，真相有三层：
- **runtime state**：ready queue、active lease、retry 调度——这是热路径的唯一真相
- **control-plane storage**：Task 元数据、Worker 注册、Rule——这是重启恢复的真相
- **trace / audit**：per-message 历史、attempt 时间线——这是分析真相，不是运行真相

三层之间有明确的放置规则，也有明确的不能越界项：control-plane 不能写高频历史，runtime 不能成为持久真相，trace 不能反向驱动运行时决策。

### TaskMsg 是一个很典型的反例

我很早就想废除 `TaskMsg` 作为热路径里的核心 owner shape，但移除不彻底，旧代码、旧测试、旧命名一直残留，它也一直牢牢占据 agent 的心智。结果是：虽然方向上想废除，但实际上它反而越加越多。

这件事前后花了超过 **两整天、40+ commits** 才真正收干净——从引入中性投影类型 `TaskMessageProjection`、到把 `TaskMsg` 降为兼容层、再到把兼容层移出 mainline 进 test sources、最终从热路径完全拔除。

这段经历让我更明确地意识到：

> 想废除一个旧主线概念，不能只在口头上废除。必须在代码、测试、文档、命名和调用路径上一起做真正收敛。口头废除但不动代码，等于给 agent 留了一个持续的错误锚点。

---

## 多个 AI 工具同时在用

这个项目里，不只是用了一个 AI。从 commit history 可以看到三个不同的 author：

- **Claude**：早期骨架搭建，大量初始代码生成
- **Copilot Agent**：PR 级别的代码 review 和修改，如 `fix: address code review`
- **我本人**：架构重构主导，每条 commit message 都是完整的 what/why/scope

这三者的分工不是预先规划的，而是自然形成的：Claude 快速出量、Copilot 做局部精修、我做架构判断和收敛。

有一个细节值得一提：**Claude 早期建立的一些模型（包括 `TaskMsg`）在后来花了大量精力清理**。这不是 Claude 的问题，而是"快速出量阶段"天然会留下这类债——你需要在后续的治理阶段把它收干净，而不是期望第一次就完全对。

---

## 后来逐渐补上的约束系统

我现在比较依赖下面几类东西来约束主线：

- **结构化日志**：帮助回放过程
- **状态审计**：帮助校对状态有没有漂
- **E2E**：保护主线行为
- **trace**：核对运行时语义
- **integration + CI**：尽早暴露偏差
- **chaos runner**：验证边界场景，比如 lease 过期后的重复回调、延迟结果不能覆盖终态
- **baseline / owner README / handoff 文档**：减少 agent 重新误解项目

这些东西不是附属品。对复杂项目来说，**它们就是高速迭代的刹车系统**。没有刹车，AI 能跑多快，就能翻多惨。

其中 AGENTS.md 这类文档在这个项目里起到的作用出乎意料地大。它不是给人看的设计文档，它是**给每个新 session 的项目宪法**——写明主线、边界、硬约束、常见误读。一个新 agent 读完它，能在 10 分钟内建立正确的全局观，而不是从代码里猜。

---

## 我最近比较强的几个感悟

### 1. 非新功能不要只加代码，不澄清旧逻辑

如果不是在做真正的新功能，而是在修正、收敛、重构主线，那就不能只往上加代码。要同时做旧逻辑澄清、路径收敛和文档同步。只加代码不澄清旧逻辑，AI 会把旧逻辑当成仍然有效的上下文继续沿用。

### 2. phase 可以有，但必须有 cleanup boundary

我不反对 phase，但必须明确：为什么暂时保留、保留到哪里、谁负责清理、什么边界一过就必须 cleanup。没有明确 boundary 的 phase，会永远是 phase。

### 3. rename 只在值得的时候做

rename 只在两类场景真正值得做：语义已经变了，或者旧命名会在热点路径上持续误导人。纯粹为了统一风格的大范围 rename，在复杂项目里性价比很低，而且会让 diff 很难读。

### 4. 不要只靠 rename 把旧路径"叫成兼容层"

把旧路径改个名字，叫成 `compatibility`、`legacy`、`bridge`，不等于它真的被收敛了。如果主线路径、调用关系、owner boundary 都还在，那这只是 relabel，不是治理。**这一点在 `TaskMsg` 的清理过程里我踩得很实**——中间有很长一段时间，它叫 "compatibility projection"，但实际上还是热路径的主角。

### 5. agent 容易形成稳定的错误倾向

这是我最近越来越在意的问题。agent 的问题不只是偶尔出错，而是会形成一些**稳定倾向**：

- 看见大类就想拆
- 看见旧逻辑就想包一层
- 看见命名不统一就先 rename
- 觉得多一层就是更优雅
- 写完测试就认为覆盖了

这些倾向在简单项目里危害有限，但在复杂项目里会系统性地破坏 owner boundary 和主线清晰度。所以复杂项目里，不只是要管理 agent 的输出，**还要管理它的思维定势**——这件事比审代码更难，因为它是隐性的。

---

## 最后

我现在对这件事的理解大概可以压成一句话：

> 复杂项目里的 vibe coding，不是把开发外包给 AI，而是把 AI 纳入一套有主线、有单一真相、有验证、有文档基线、有纠偏机制的工程系统。

如果只看"写得快"，复杂度迟早会反噬。

如果把主线、truth、测试、trace、文档和 review 机制搭起来，AI 才真正有可能成为复杂项目的迭代放大器——不只是让第一步走得快，而是让第一百步还走得稳。

---
