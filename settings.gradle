pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = 'xa-mass-platform'

include ':foundation_jvm'
include ':kernel_jvm'
include ':server_jvm'
include ':worker_delivery_contract_jvm'
include ':transport:core'
include ':transport:netty-adapter'
include ':transport:okhttp-worker'
include ':transport:android-client'
