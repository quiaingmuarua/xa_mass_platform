mass:
  storage:
    mode: memory
  auth:
    operator:
      mode: dev-header
      allow-local-fixture-header: true
  demo:
    bootstrap:
      enabled: false
sample:
  bootstrap:
    enabled: true
