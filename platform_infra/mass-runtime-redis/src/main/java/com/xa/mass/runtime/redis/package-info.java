/**
 * Redis-backed runtime infrastructure for the shared platform-infra module
 * family. The current slice fixes keyspace and index ownership only; queue and
 * lease operations are still future work.
 */
package com.xa.mass.runtime.redis;
