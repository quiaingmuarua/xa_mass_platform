<?xml version="1.0" encoding="UTF-8"?>
<!--
  Consumer POM template for local/internal staging of xa-mass-java-sdk.

  The reactor pom.xml intentionally inherits the platform parent. This file is
  the reviewed standalone metadata shape that external consumers should see if
  the SDK is published outside the platform reactor. Public registry metadata
  such as SCM, developers, signing, and a final license must be completed when
  a public registry target is approved.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.xa.mass</groupId>
    <artifactId>xa-mass-java-sdk</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>xa-mass-java-sdk</name>
    <description>External JVM SDK for a running XA Mass server.</description>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <jackson.version>2.17.1</jackson.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson.version}</version>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-jsr310</artifactId>
            <version>${jackson.version}</version>
        </dependency>
    </dependencies>
</project>
