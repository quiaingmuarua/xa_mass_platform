{
  "rules": [
    {
      "id": "basic_worker_check",
      "name": "Basic worker check",
      "description": "Worker scheduling resource identity must be known",
      "type": "QL_EXPRESS",
      "content": "hasWorkerSchedulingResource == true",
      "priority": 1,
      "enabled": true
    },
    {
      "id": "target_worker_attributes_check",
      "name": "Target worker attributes check",
      "description": "Target worker attributes, when required, must match worker attributes",
      "type": "QL_EXPRESS",
      "content": "matchesTargetWorkerAttributes == true",
      "priority": 2,
      "enabled": true
    },
    {
      "id": "routing_country_match",
      "name": "Routing country match",
      "description": "Routing country, when required, must match worker scheduling routing tags",
      "type": "QL_EXPRESS",
      "content": "taskHasRoutingRequirement == false || workerSchedulingMatchesRoutingCode == true",
      "priority": 3,
      "enabled": true
    }
  ]
}
