plugins {
    id 'java'
}

base {
    archivesName.set('xa-mass-worker-capability-rpc-integration')
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

dependencies {
    implementation project(':worker_delivery_contract_jvm')

    testImplementation platform('org.junit:junit-bom:5.12.2')
    testImplementation 'org.junit.jupiter:junit-jupiter'
    testImplementation 'com.googlecode.libphonenumber:libphonenumber:9.0.31'
    testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
}

tasks.register('runRpcScenario', JavaExec) {
    description = 'Runs the scenario Worker capability RPC proof.'
    group = 'application'
    classpath = sourceSets.main.runtimeClasspath
    mainClass = 'com.xa.mass.integration.workercapability.WorkerCapabilityRpcMain'
    workingDir = project.projectDir
}

tasks.named('test') {
    useJUnitPlatform()
    workingDir = project.projectDir
}
