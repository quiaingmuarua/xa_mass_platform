Java群控后台框架开发规范（Draft）
日志输出规范
项目中打印信息统一使用日志（如log.info等），严禁使用System.out.println等直接输出方式。

代码编译要求
代码每次修改后，须重新执行Maven编译，确保变更生效。

重大变更文档记录
框架结构、大模块调整，需同步维护专门的文档说明，便于后续业务接入和团队成员了解。

代码合并与审核
代码合并前需完成Code Review，并通过静态代码检查，无严重告警后方可合并。

接口与方法注释

所有对外暴露的接口（public API）必须补充详细注释，说明用途、参数、返回值、异常等。

核心方法（包括策略、调度、client管理等核心逻辑）须补充注释，方便维护和后续业务开发。

函数式编程优先
鼓励优先使用Java 8+的函数式接口和编程范式（如Function、Supplier、Consumer、Stream等），提升代码简洁性和扩展性。

核心资源空值处理
业务核心资源（如client、调度任务等）为null时，不应简单判空和兼容处理，必须直接抛出业务异常，由统一异常处理机制接管，防止隐患扩散。

异常处理机制
所有异常应抛出框架定义的统一异常类型（如BizException），并通过统一异常处理接口进行捕获和日志记录，禁止无脑try-catch吞异常。

配置变更登记
所有框架配置变更（如参数、注解、默认策略等）需在配置文档/变更记录中登记，方便业务方查阅。

测试用例完善
每个核心模块须配备基础单元测试，并保证主要接口有测试覆盖，防止后续扩展引发回归问题。

