[
  {
    "MODEL": "RuleDefinition",
    "COUNT": 1,
    "FIELDS": {
      "id": "basic_worker_check",
      "name": "Basic worker check",
      "description": "Worker must be online and unlocked",
      "desc": "Worker must be online and unlocked",
      "type": "QL_EXPRESS",
      "content": "isWorkerAvailable == true && isWorkerLocked == false",
      "priority": 1,
      "enabled": true
    }
  },
  {
    "MODEL": "RuleDefinition",
    "COUNT": 1,
    "FIELDS": {
      "id": "worker_context_status_check",
      "name": "Worker context status check",
      "description": "Worker without context is allowed; otherwise worker context must be allocatable",
      "desc": "Worker without context is allowed; otherwise worker context must be allocatable",
      "type": "QL_EXPRESS",
      "content": "hasWorkerContext == false || isWorkerContextAllocatable == true",
      "priority": 2,
      "enabled": true
    }
  },
  {
    "MODEL": "RuleDefinition",
    "COUNT": 1,
    "FIELDS": {
      "id": "routing_country_match",
      "name": "Routing country match",
      "description": "Routing country, when required, must come from worker context",
      "desc": "Routing country, when required, must come from worker context",
      "type": "QL_EXPRESS",
      "content": "taskHasRoutingRequirement == false || workerContextAttributeCountryMatchesRoutingCode == true || workerContextChannelMatchesRoutingCode == true",
      "priority": 3,
      "enabled": true
    }
  },
  {
    "MODEL": "RuleDefinition",
    "COUNT": 1,
    "FIELDS": {
      "id": "app_support_check",
      "name": "App support check",
      "description": "Worker must support the task project",
      "desc": "Worker must support the task project",
      "type": "QL_EXPRESS",
      "content": "supportsProject == true",
      "priority": 4,
      "enabled": true
    }
  },
  {
    "MODEL": "RuleDefinition",
    "COUNT": 1,
    "FIELDS": {
      "id": "worker_load_check",
      "name": "Worker load check",
      "description": "Worker-supported project count must stay below 10",
      "desc": "Worker-supported project count must stay below 10",
      "type": "QL_EXPRESS",
      "content": "appCount < 10",
      "priority": 5,
      "enabled": true
    }
  }
]
